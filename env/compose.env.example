# Copy to /opt/anytype-vault/env/compose.env and edit values.
# Used by anytype-vault.service to pass environment into docker compose.

# VM Tailscale IPv4 (from: tailscale ip -4)
TAILSCALE_IP="100.x.y.z"
